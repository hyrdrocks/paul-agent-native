{
  "$comment": "Ratchet baseline for guard:template-standard. Every entry is a known, pre-existing template-standard gap accepted for now (Phase 1 does not edit templates/*). The guard only fails on NEW drift not listed here. Fix the underlying template and delete the entry instead of leaving it here once addressed. See scripts/template-standard/baseline.ts for the reconciliation rules.",
  "violations": [
    {
      "rule": "byte-sync:learnings-defaults:mismatch",
      "template": "analytics",
      "note": "Extended the shared scaffold with real app-specific learnings (metric definitions, schema/provider gotchas). Content divergence is intentional, not a defect — Phase 2/3 should decide whether byte-exact is even the right rule for a file the agent is meant to keep updating."
    },
    {
      "rule": "byte-sync:learnings-defaults:mismatch",
      "template": "clips",
      "note": "Extended the shared scaffold (and reworded its header) with real app-specific learnings. Same reasoning as analytics — intentional customization, not a gap to silently overwrite."
    },
    {
      "rule": "byte-sync:gitignore-source:mismatch",
      "template": "assets",
      "note": "Existing _gitignore has legitimate template-specific additions (.generated/, .agent-native/, data/*.db*) on top of the shared base. Needs a human decision in Phase 2/3: reconcile into the canonical file or keep as an accepted per-template diff."
    },
    {
      "rule": "byte-sync:gitignore-source:mismatch",
      "template": "brain",
      "note": "Existing _gitignore predates the canonical shape (different section ordering/entries). Needs reconciliation in Phase 2/3."
    },
    {
      "rule": "byte-sync:gitignore-source:mismatch",
      "template": "chat",
      "note": "Existing _gitignore adds .generated/ and data/*.db* on top of the shared base. Needs reconciliation in Phase 2/3."
    },
    {
      "rule": "byte-sync:gitignore-source:mismatch",
      "template": "crm",
      "note": "Existing _gitignore predates the canonical shape (different section ordering/entries). Needs reconciliation in Phase 2/3."
    },
    {
      "rule": "byte-sync:gitignore-source:mismatch",
      "template": "design",
      "note": "Existing _gitignore adds Playwright E2E artifact paths on top of the shared base — legitimate template-specific addition. Needs a human decision in Phase 2/3 on reconciling vs. keeping as an accepted diff."
    },
    {
      "rule": "byte-sync:gitignore-source:mismatch",
      "template": "dispatch",
      "note": "Existing _gitignore adds .generated/ and data/*.db* on top of the shared base. Needs reconciliation in Phase 2/3."
    },
    {
      "rule": "byte-sync:gitignore-source:mismatch",
      "template": "plan",
      "note": "Existing _gitignore adds .generated/ and data/*.db* on top of the shared base. Needs reconciliation in Phase 2/3."
    },
    {
      "rule": "byte-sync:gitignore-source:mismatch",
      "template": "slides",
      "note": "Slides stores development-only PPTX import fallback assets under data/import-assets/, so its generated-app ignore list intentionally extends the shared base."
    }
  ]
}
