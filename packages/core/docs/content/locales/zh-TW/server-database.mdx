---
title: "資料庫"
description: "將便攜式 SQL 資料庫連線到您的 Agent-Native 應用：架構幫手、遷移，以及託管資料庫選項。"
---

# 資料庫

每個 Agent-Native 應用都將其狀態儲存在 SQL 中。UI 與 agent 都透過同一組 actions，使用同一個 [Drizzle ORM](https://orm.drizzle.team) 用戶端，讀寫相同的資料表。當任一方變更資料時，另一方會透過輪詢層看到變化。

<Diagram id="doc-block-9mtx2f" title="資料如何流向資料庫" summary="UI 與 agent 都透過同一個 Actions 層存取資料庫。兩者都使用相同的 getDb() 用戶端與相同的 SQL 資料表。並不存在各自獨立的後端。">

```html
<div class="diagram-dbr">
  <div class="diagram-col">
    <div class="diagram-node">瀏覽器 / UI</div>
    <div class="diagram-node">Agent 迴圈</div>
  </div>
  <div class="diagram-arrow diagram-muted" aria-hidden="true">&rarr;</div>
  <div class="diagram-panel" data-rough>
    <strong>Actions</strong>
    <small class="diagram-muted">defineAction</small>
    <small class="diagram-muted">getDb() &middot; Drizzle ORM</small>
  </div>
  <div class="diagram-arrow diagram-muted" aria-hidden="true">&rarr;</div>
  <div class="diagram-box" data-rough>
    SQL 資料庫<br /><small class="diagram-muted">方言取自 DATABASE_URL</small>
  </div>
</div>
```

```css
.diagram-dbr {
  display: flex;
  align-items: center;
  gap: 14px;
  flex-wrap: wrap;
}
.diagram-dbr .diagram-col {
  display: flex;
  flex-direction: column;
  gap: 10px;
}
.diagram-dbr .diagram-panel {
  display: flex;
  flex-direction: column;
  gap: 4px;
  padding: 12px 16px;
}
.diagram-dbr .diagram-arrow {
  font-size: 22px;
  line-height: 1;
}
```

</Diagram>

## 託管選項 {#hosting}

應用會從 `DATABASE_URL` 偵測要使用哪個後端。當該變數未設定時，會回退到本機 SQLite 檔案，讓您無需任何設定即可開始開發。

### 預設：SQLite 檔案 {#default-sqlite}

當 `DATABASE_URL` 未設定時，應用會在 `data/app.db` 建立一個 SQLite 資料庫。無需任何設定。啟動開發伺服器後，該檔案會在第一次執行時建立。

這僅適用於本機開發。容器、無伺服器函式與預覽環境可能會在重啟之間重設其檔案系統，因此本機 SQLite 檔案可能會消失。部署前請將 `DATABASE_URL` 設定為持久化的託管資料庫。

### 本機 Postgres：PGlite {#local-pglite}

若要在不使用 Docker 或託管資料庫的情況下，於本機以 Postgres 方言開發，請安裝選用的 PGlite 套件並設定 `DATABASE_URL`：

```bash
pnpm add @electric-sql/pglite@^0.5.3
```

```bash
DATABASE_URL=pglite:./data/pglite
```

[PGlite](https://pglite.dev/) 在行程內執行一個 WASM 版本的 Postgres 資料庫。它讓您能在部署前就抓到僅限 Postgres 的架構問題,同時維持與 SQLite 預設值一樣簡單的設定方式。和 SQLite 一樣，它僅是本機儲存,請勿用於正式環境或共用環境。

### 正式環境資料庫 {#production}

在 `.env` 檔案或部署提供者的環境變數中設定 `DATABASE_URL`，以連線託管資料庫：

```bash
# Neon Postgres 資料庫
DATABASE_URL=postgres://user:pass@ep-cool-name-123456.us-east-2.aws.neon.tech/mydb?sslmode=require

# Supabase Postgres 資料庫
DATABASE_URL=postgres://postgres.xxxx:pass@aws-0-us-east-1.pooler.supabase.com:6543/postgres

# 普通 Postgres 資料庫
DATABASE_URL=postgres://user:pass@localhost:5432/mydb

# Turso（libSQL）。也需要 DATABASE_AUTH_TOKEN。
DATABASE_URL=libsql://my-db-org.turso.io
```

框架會自動從 URL 前綴偵測方言，並據此設定 Drizzle。

### Builder.io 託管資料庫 {#builder-managed}

*計畫中（尚不可用）：*連線到 Builder.io 後，您的應用將能使用自動佈建的託管資料庫，無需任何連線字串。

## 設定資料庫 {#setup}

使用資料庫的應用需要三個檔案：

- `server/db/schema.ts`：資料表定義
- `server/db/index.ts`：具型別的資料庫用戶端單例
- `server/plugins/db.ts`：在啟動時執行的遷移

### 1. 定義您的架構 {#schema}

從 `@agent-native/core/db/schema` 匯入架構幫手。切勿直接從 `drizzle-orm/sqlite-core` 或 `drizzle-orm/pg-core` 匯入。框架幫手會產生與方言無關的定義，可在所有支援的後端上運作。

```ts filename="server/db/schema.ts"
import { integer, now, table, text } from "@agent-native/core/db/schema";

export const tasks = table("tasks", {
  id: text("id").primaryKey(),
  title: text("title").notNull(),
  priority: integer("priority").notNull().default(0),
  done: integer("done", { mode: "boolean" }).notNull().default(false),
  ownerEmail: text("owner_email").notNull(),
  createdAt: text("created_at").notNull().default(now()),
});
```

| 幫手      | 目的                                                                   |
| --------- | ---------------------------------------------------------------------- |
| `table`   | 定義一個資料表；委派給 `pgTable` 或 `sqliteTable`                      |
| `text`    | 文字欄，支援 `{ enum: [...] }`                                         |
| `integer` | 整數欄，`{ mode: "boolean" }` 對應到 Postgres 的布林值                 |
| `real`    | 浮點欄。在 SQLite 上對應 `real`，在 Postgres 上對應 `double precision` |
| `now`     | 與方言無關、供 `.default(now())` 使用的目前時間戳                      |

<DataModel
  id="doc-block-3hbven"
  title="tasks 資料表"
  summary={"只需以框架幫手定義一次；方言會在執行時從 DATABASE_URL 選定。"}
  entities={[
    {
      id: "tasks",
      name: "tasks",
      note: "領域資料表。新增 owner_email（或 ...ownableColumns()），讓 SQL 層級的範圍限定能將行篩選給經驗證的使用者。",
      fields: [
        {
          name: "id",
          type: "text",
          pk: true,
          nullable: false,
        },
        {
          name: "title",
          type: "text",
          nullable: false,
        },
        {
          name: "priority",
          type: "integer",
          nullable: false,
          note: "預設為 0",
        },
        {
          name: "done",
          type: "integer（boolean 模式）",
          nullable: false,
          note: "預設為 false；對應到 Postgres 的布林值",
        },
        {
          name: "owner_email",
          type: "text",
          nullable: false,
          note: "啟用資料範圍限定",
        },
        {
          name: "created_at",
          type: "text",
          nullable: false,
          note: "預設為 now()",
        },
      ],
    },
  ]}
/>

儲存單一使用者資料的資料表必須包含 `owner_email` 欄，以便框架能將行篩選給經驗證的使用者。同時支援與其他使用者或組織共用的資料表，則應改用展開 `...ownableColumns()`，它會在一次呼叫中新增 `owner_email`、`org_id` 和 `visibility`。詳見下方 [將資料範圍限定給使用者](#scoping)。

### 2. 建立資料庫用戶端 {#db-client}

每個應用透過呼叫 `createGetDb(schema)` 來建立一個惰性的單例 Drizzle 用戶端。規範位置是 `server/db/index.ts`：

```ts filename="server/db/index.ts"
import { createGetDb } from "@agent-native/core/db";
import * as schema from "./schema.js";

export const getDb = createGetDb(schema);
```

`createGetDb` 回傳一個 `getDb()` 函式，該函式會在第一次呼叫時開啟資料庫連線，並在後續呼叫時回傳同一個具型別的 Drizzle 實例。它會在執行時讀取 `DATABASE_URL`，以決定要使用哪個後端與方言。

在 actions 與路由中，從這個範本本機路徑匯入 `getDb`。不要直接從 `@agent-native/core` 匯入。核心匯出是無型別的；本機匯出則帶有您的架構型別。

### 3. 撰寫遷移 {#migrations}

架構變更是透過 `server/plugins/db.ts` 中的 Nitro 外掛在啟動時套用的。請使用 `@agent-native/core/db` 中的 `runMigrations`：

<AnnotatedCode
  id="doc-block-7qwlpz"
  title="啟動時的遷移外掛"
  filename="server/plugins/db.ts"
  language="ts"
  code={
    'import { runMigrations } from "@agent-native/core/db";\n\nexport default runMigrations(\n  [\n    {\n      version: 1,\n      sql: `CREATE TABLE IF NOT EXISTS tasks (\n        id TEXT PRIMARY KEY,\n        title TEXT NOT NULL,\n        done INTEGER NOT NULL DEFAULT 0,\n        owner_email TEXT NOT NULL,\n        created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP\n      )`,\n    },\n    {\n      version: 2,\n      sql: `ALTER TABLE tasks ADD COLUMN IF NOT EXISTS priority INTEGER NOT NULL DEFAULT 0`,\n    },\n    {\n      // Dialect-gated: runs only on the matching backend.\n      version: 3,\n      sql: {\n        postgres: `ALTER TABLE tasks ADD COLUMN IF NOT EXISTS tsv tsvector`,\n        sqlite: `SELECT 1`,\n      },\n    },\n  ],\n  { table: "my_app_migrations" },\n);'
  }
  annotations={[
    {
      lines: "6",
      label: "每個應用程式獨立的版本表",
      note: "每個應用都在各自獨立的資料表中追蹤自己已套用的版本。請使用您應用專屬的名稱，以免與框架遷移或共用同一資料庫的其他應用衝突。",
    },
    {
      lines: "7",
      label: "初始建表",
      note: "`CREATE TABLE IF NOT EXISTS` 可以在每次重啟時安全地重新執行。請將完整的資料表定義放在版本 1。",
    },
    {
      lines: "17",
      label: "附加式新增欄位",
      note: "`ADD COLUMN IF NOT EXISTS` 是在初次建表後新增欄位的安全做法。切勿刪除或重新命名欄位。",
    },
    {
      lines: "22-25",
      label: "方言門控",
      note: "傳入一個以方言為鍵的物件，即可依後端執行不同的 SQL。對不需要此變更的方言，使用 `SELECT 1` 作為空操作。",
    },
  ]}
/>

`runMigrations` 會依序執行每個項目一次，然後記錄該版本，以便在往後的重啟中跳過。每個項目都是冪等的。此外掛可以在每次啟動時安全呼叫。

<Callout tone="risk">
  切勿針對正式環境資料庫執行 `drizzle-kit
  push`。範本架構僅定義特定於應用程式的資料表；它們不包含中央框架資料表（`user`、`session`、`application_state`
  等）。若針對正式環境執行 `drizzle-kit
  push`，會將這些資料表偵測為「不在架構中」並嘗試刪除它們，造成立即的資料遺失。
</Callout>

每個應用根目錄下的 `drizzle.config.ts` 會為本機開發時的架構檢視設定 drizzle-kit：

```ts filename="drizzle.config.ts"
import { createDrizzleConfig } from "@agent-native/core/db/drizzle-config";
export default createDrizzleConfig();
```

使用 `pnpm db:generate` 檢視您的架構，並僅針對本機資料庫使用 `pnpm db:push`。

### 4. 在 actions 中查詢 {#querying}

從您的 actions 中呼叫 `getDb()` 以取得具型別的 Drizzle 用戶端。使用 Drizzle 的查詢建構器與 `drizzle-orm` 中的可移植運算子：

```ts
import { and, desc, eq } from "drizzle-orm";
import { getDb } from "../server/db/index.js";
import * as schema from "../server/db/schema.js";

const db = getDb();

const openTasks = await db
  .select()
  .from(schema.tasks)
  .where(
    and(eq(schema.tasks.ownerEmail, userEmail), eq(schema.tasks.done, false)),
  )
  .orderBy(desc(schema.tasks.createdAt));

await db
  .update(schema.tasks)
  .set({ done: true })
  .where(eq(schema.tasks.id, taskId));
```

## 將資料範圍限定給使用者 {#scoping}

所有針對面向使用者資料表的讀寫，都必須限定在經驗證的使用者範圍內。框架依資料是私有還是可共用，提供兩種模式。

**私有資料：**屬於單一使用者的資料表。在架構中加入 `ownerEmail: text("owner_email").notNull()`，並在每個查詢中包含 `eq(table.ownerEmail, userEmail)`：

```ts
.where(eq(schema.tasks.ownerEmail, userEmail))
```

**共用資源：**可與其他使用者或組織共用的資料表。請在架構中展開 `...ownableColumns()`，而不是單純的 `owner_email`。這會在一次呼叫中新增 `owner_email`、`org_id` 和 `visibility`，並用 `createSharesTable` 建立一個對應的分享資料表：

```ts filename="server/db/schema.ts"
import {
  table,
  text,
  ownableColumns,
  createSharesTable,
} from "@agent-native/core/db/schema";

export const decks = table("decks", {
  id: text("id").primaryKey(),
  title: text("title").notNull(),
  ...ownableColumns(),
});
export const deckShares = createSharesTable("deck_shares");
```

接著在列表查詢中使用 `@agent-native/core/sharing` 中的 `accessFilter`，取代手動的 `eq` 檢查：

```ts
import { accessFilter } from "@agent-native/core/sharing";

const rows = await db
  .select()
  .from(schema.decks)
  .where(accessFilter(schema.decks, schema.deckShares));
```

`accessFilter` 建構的查詢會納入呼叫者擁有的行、與其組織共用的行,以及明確與其分享的行。它不會暴露其他使用者的行。

完整模型請參閱 [Security — 資料範圍限定](/docs/security#data-scoping) 與 [Sharing](/docs/sharing#building)。

## 由 SQL 支援的同步 {#sync}

Agent-Native 不仰賴檔案系統監看器或黏著式的記憶體內狀態。當某個 action 寫入資料庫時，一個同步版本號會遞增。用戶端的 `useDbSync()` hook 會輪詢 `/_agent-native/poll`，當偵測到更高的版本號時，使 React Query 快取失效。

由於資料庫是協調點，這在無伺服器與多實例部署間都能運作。若您在 actions 之外撰寫自訂的變更操作，請使用框架幫手，或發出對應的同步失效通知，讓開啟中的 UI 得以重新整理。

<Diagram id="doc-block-qhkdv8" title="由 SQL 支援的同步迴圈" summary="沒有監看器，沒有黏著式狀態。一次寫入會使 SQL 中的版本號遞增；每個用戶端輪詢該版本號並重新取得資料。">

```html
<div class="diagram-db-sync">
  <div class="diagram-box" data-rough>
    Action / 幫手<br /><small class="diagram-muted">變更資料</small>
  </div>
  <div class="diagram-arrow diagram-muted" aria-hidden="true">&rarr;</div>
  <div class="diagram-panel" data-rough>
    <strong>SQL 資料庫</strong
    ><small class="diagram-muted">同步版本號遞增</small>
  </div>
  <div class="diagram-arrow diagram-muted" aria-hidden="true">&larr;</div>
  <div class="diagram-col">
    <div class="diagram-node">
      useDbSync()<br /><small class="diagram-muted"
        >輪詢 /_agent-native/poll</small
      >
    </div>
    <div class="diagram-pill ok">使快取失效 &rarr; UI 重新整理</div>
  </div>
</div>
```

```css
.diagram-db-sync {
  display: flex;
  align-items: center;
  gap: 14px;
  flex-wrap: wrap;
}
.diagram-db-sync .diagram-col {
  display: flex;
  flex-direction: column;
  gap: 8px;
  align-items: flex-start;
}
.diagram-db-sync .diagram-arrow {
  font-size: 22px;
  line-height: 1;
}
```

</Diagram>

<Endpoint id="doc-block-1s11d2e" title="輪詢端點" method="GET" path="/_agent-native/poll" summary="回傳目前每個來源的資料庫同步版本號，讓用戶端能偵測變化。" auth="工作階段 cookie（請求範圍的身分）" responses={[
  {
    "status": "200",
    "description": "以來源為鍵的目前同步版本號。"
  }
]}>

`useDbSync()` 會按固定間隔呼叫此端點，並在 SSE 無法使用時回退至此。當回傳的版本號高於用戶端最後看到的值時，對應的 React Query 快取會失效並重新取得。

</Endpoint>

## 原始 SQL {#raw-sql}

對於 Drizzle 查詢建構器無法表達的進階查詢、健康檢查或一次性維護作業，請使用 `@agent-native/core/db` 中的 `getDbExec`：

```ts
import { getDbExec, isPostgres, intType } from "@agent-native/core/db";

const { rows } = await getDbExec().execute({
  sql: `SELECT id, title FROM tasks WHERE owner_email = ? LIMIT ?`,
  args: [userEmail, 50],
});
```

`getDbExec` 會自動將 `?` 參數轉換成 Postgres 的 `$1`、`$2` 等格式。使用 `isPostgres()` 依方言分支處理，並用 `intType()` 回傳目前後端對應的正確整數型別。一般讀寫請優先選用 Drizzle 查詢建構器；原始 SQL 會繞過型別安全，也更難維護。

## 環境變數 {#environment-variables}

| 變數                  | 目的                                                                                      |
| --------------------- | ----------------------------------------------------------------------------------------- |
| `DATABASE_URL`        | 持久化 SQL 連線字串（未設定 = 本機 SQLite；`pglite:./data/pglite` = 選用的本機 Postgres） |
| `DATABASE_AUTH_TOKEN` | 需要獨立權杖的提供者所使用的驗證權杖，例如 Turso/libSQL                                   |

## 下一步是什麼 {#whats-next}

- [**Security — 資料範圍限定**](/docs/security#data-scoping)：`owner_email` 與存取幫手如何限定讀寫範圍
- [**Sharing**](/docs/sharing#building)：`ownableColumns()` 與共用資源的可見性模型
- [**外掛**](/docs/server-plugins)：遷移執行所在的啟動外掛生命週期
- [**Actions**](/docs/actions)：actions 呼叫 `getDb()` 讀寫資料的介面
- [**部署**](/docs/deployment#persistent-database)：為每個部署目標連線持久化資料庫
- [**即時同步**](/docs/key-concepts#polling-sync)：`useDbSync()` 與完整的用戶端同步模型
