---
title: "النشر"
description: "انشر تطبيقات الوكيل الأصلية على أي نظام أساسي باستخدام إعدادات Nitro المسبقة - Node.js، وVercel، وNetlify، وCloudflare، وAWS، والمزيد."
---

# النشر

تستخدم التطبيقات الأصلية للوكيل [Nitro](https://nitro.build) تحت الغطاء، مما يعني أنه يمكنك النشر على أي نظام أساسي بدون أي تغييرات في التكوين — فقط قم بتعيين إعداد مسبق.

## قبل النشر: اختر قاعدة بيانات ثابتة {#persistent-database}

يحتاج كل تطبيق منشور إلى قاعدة بيانات SQL مستمرة. في التطوير المحلي، يعود Agent-Native إلى ملف SQLite في `data/app.db`؛ وهو مناسب لجهازك، ولكنه ليس دائمًا في الحاويات أو المعاينات أو البيئات التي لا تحتوي على خادم حيث يمكن إعادة ضبط نظام الملفات.

قم بتعيين `DATABASE_URL` في موفر النشر الخاص بك قبل ترقية التطبيق إلى مرحلة الإنتاج. يستخدم Agent-native Drizzle للمخطط والاستعلامات، وبالتالي تكون طبقة البيانات قابلة للنقل عبر واجهات SQL الخلفية المتوافقة مع Drizzle ويكتشف إطار العمل اللهجة تلقائيًا من URL. راجع [Database](/docs/database#production) للحصول على قائمة المحولات وتفاصيل اللهجات.

استخدم `DATABASE_AUTH_TOKEN` فقط عندما يطلب موفر قاعدة البيانات رمزًا مميزًا منفصلاً، مثل Turso/libSQL. بالنسبة لمساحات العمل، تكتسب جميع التطبيقات الجذر `DATABASE_URL` افتراضيًا؛ قم بتعيين `<APP_NAME>_DATABASE_URL` عندما يجب أن يستخدم أحد التطبيقات قاعدة بيانات مختلفة.

## نشر مساحة العمل: أصل واحد، تطبيقات متعددة {#workspace-deploy}

إذا كان مشروعك [workspace](/docs/multi-app-workspace)، فيمكنك شحن كل تطبيق فيه إلى أصل واحد باستخدام أمر واحد:

```bash
npx @agent-native/core@latest deploy
# https://your-agents.com/mail/*       → apps/mail
# https://your-agents.com/calendar/*   → apps/calendar
# https://your-agents.com/forms/*      → apps/forms
```

يتم إنشاء كل تطبيق باستخدام `APP_BASE_PATH=/<name>` و`VITE_APP_BASE_PATH=/<name>`، ثم يتم تجميعه للإعداد المسبق المستهدف Nitro. يعد Cloudflare Pages هو الإعداد المسبق الافتراضي ويستخدم عامل إرسال تم إنشاؤه في `dist/_worker.js`؛ يستخدم Netlify وظيفة واحدة لكل تطبيق في `.netlify/functions-internal/<app>-server` بالإضافة إلى عمليات إعادة التوجيه التي تم إنشاؤها؛ يكتب Vercel `.vercel/output` على مستوى مساحة العمل باستخدام Build Output API.

<Diagram id="doc-block-rxsqim" title={"أصل واحد، والعديد من التطبيقات"} summary={"يتم إنشاء كل تطبيق مساحة عمل بمسار أساسي خاص به ويتم تثبيته أسفل بادئة مسار على أصل واحد - لذا فإن تسجيل الدخول والتطبيقات المشتركة A2A هما نفس الأصل ومجاني."}>

```html
<div class="diagram-ws">
  <div class="diagram-panel" data-rough>
    <strong>https://your-agents.com</strong>
    <div class="diagram-row">
      <span class="diagram-pill accent">/mail/*</span
      ><small class="diagram-muted">apps/mail</small>
    </div>
    <div class="diagram-row">
      <span class="diagram-pill accent">/calendar/*</span
      ><small class="diagram-muted">apps/calendar</small>
    </div>
    <div class="diagram-row">
      <span class="diagram-pill accent">/forms/*</span
      ><small class="diagram-muted">apps/forms</small>
    </div>
  </div>
  <div class="diagram-col wins">
    <span class="diagram-pill ok">shared login session</span
    ><span class="diagram-pill ok">A2A بين التطبيقات بلا إعداد</span>
  </div>
</div>
```

```css
.diagram-ws {
  display: flex;
  align-items: center;
  gap: 16px;
  flex-wrap: wrap;
}
.diagram-ws .diagram-panel {
  display: flex;
  flex-direction: column;
  gap: 6px;
  padding: 14px 16px;
}
.diagram-ws .diagram-row {
  display: flex;
  align-items: center;
  gap: 8px;
}
.diagram-ws .wins {
  display: flex;
  flex-direction: column;
  gap: 8px;
  align-items: flex-start;
}
```

</Diagram>

يمنحك النشر بنفس المصدر فوزين كبيرين مجانًا:

- **جلسة تسجيل الدخول المشتركة** — قم بتسجيل الدخول إلى أي تطبيق، حيث يتم تسجيل الدخول إلى كل تطبيق.
- **A2A عبر التطبيقات ذات التكوين الصفري** — وضع العلامات على `@calendar` من البريد هو عملية جلب من نفس المصدر؛ لا يوجد CORS ولا يوجد توقيع JWT بين الأشقاء.

انشر المخرجات باستخدام:

```bash
wrangler pages deploy dist
```

بالنسبة لعمليات النشر الموحدة لـ Netlify، استخدم الإعداد المسبق لـ Netlify:

```bash
npx @agent-native/core@latest deploy --preset netlify
```

بالنسبة لعمليات النشر الموحدة لـ Vercel، استخدم إعداد Vercel المسبق:

```bash
npx @agent-native/core@latest deploy --preset vercel
```

عند تكوين أمر بناء الموفر، استخدم نفس الأمر مع `--build-only`. يجب أن يقوم Vercel بتشغيل `npx @agent-native/core@latest deploy --preset vercel --build-only`؛ يقوم الأمر بكتابة `.vercel/output` مباشرة، لذلك لا يلزم وجود `vercel.json` لتوجيه مساحة العمل.

تتطلب إصدارات مساحة العمل المستضافة وجود `A2A_SECRET` في بيئة موفر النشر.
يؤدي هذا إلى استئناف عمل Slack وwebhooks الداخلي وA2A عبر التطبيقات من خلال التوقيع
معالجات الخلفية. لا تزال عمليات التحقق من العناصر المحلية `--build-only` تعمل بدونها.

لا يزال النشر المستقل لكل تطبيق مدعومًا - فقط `cd apps/<name> && npx @agent-native/core@latest build` مثل سقالة مستقلة.

## كيفية العمل {#how-it-works}

عند تشغيل `npx @agent-native/core@latest build`، يقوم Nitro ببناء كل من العميل SPA والخادم API في `.output/`:

<FileTree
  id="doc-block-iw2wb5"
  title={"مخرجات build"}
  entries={[
    {
      path: ".output/",
      note: "مكتف ذاتياً: انسخه إلى أي environment وشغّله",
    },
    {
      path: ".output/public/",
      note: "SPA مبنية (static assets)",
    },
    {
      path: ".output/server/index.mjs",
      note: "نقطة دخول server",
    },
    {
      path: ".output/server/chunks/",
      note: "أجزاء كود الخادم",
    },
  ]}
/>

الإخراج مستقل بذاته - انسخ `.output/` إلى أي بيئة وقم بتشغيله.

<Diagram id="doc-block-1dya5o5" title={"بناء للنشر"} summary={"يتم بناء شجرة مصدر واحدة على إعداد مسبق Nitro؛ يتم تشغيل نفس الإخراج المستقل على Node، أو Vercel، أو Netlify، أو Cloudflare، أو AWS، أو Deno. يشير كل مثيل إلى نفس DATABASE_URL المستمر."}>

```html
<div class="diagram-deploy">
  <div class="diagram-box" data-rough>مصدر التطبيق</div>
  <div class="diagram-arrow diagram-muted" aria-hidden="true">&rarr;</div>
  <div class="diagram-panel center" data-rough>
    <span class="diagram-pill accent">build</span
    ><small class="diagram-muted">Nitro preset</small>
  </div>
  <div class="diagram-arrow diagram-muted" aria-hidden="true">&rarr;</div>
  <div class="diagram-grid">
    <span class="diagram-pill">Node.js</span
    ><span class="diagram-pill">Vercel</span
    ><span class="diagram-pill">Netlify</span
    ><span class="diagram-pill">Cloudflare</span
    ><span class="diagram-pill">AWS Lambda</span
    ><span class="diagram-pill">Deno</span>
  </div>
  <div class="diagram-arrow diagram-muted" aria-hidden="true">&rarr;</div>
  <div class="diagram-box" data-rough>
    DATABASE_URL دائم<br /><small class="diagram-muted"
      >shared by every instance</small
    >
  </div>
</div>
```

```css
.diagram-deploy {
  display: flex;
  align-items: center;
  gap: 12px;
  flex-wrap: wrap;
}
.diagram-deploy .center {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 4px;
  padding: 14px 16px;
}
.diagram-deploy .diagram-arrow {
  font-size: 22px;
  line-height: 1;
}
.diagram-deploy .diagram-grid {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 8px;
}
```

</Diagram>

## ضبط الإعداد المسبق {#setting-the-preset}

افتراضيًا، يتم إنشاء Nitro لـ Node.js. لاستهداف منصة مختلفة، قم بضبط الإعداد المسبق في `vite.config.ts`:

```ts
import { agentNative } from "@agent-native/core/vite";
import { defineConfig } from "vite";

export default defineConfig({
  plugins: [agentNative({ nitro: { preset: "vercel" } })],
});
```

أو استخدم متغير البيئة `NITRO_PRESET` في وقت الإنشاء:

```bash
NITRO_PRESET=netlify npx @agent-native/core@latest build
```

## Node.js (افتراضي) {#nodejs}

الإعداد المسبق الافتراضي. البناء والتشغيل:

```bash
npx @agent-native/core@latest build
node .output/server/index.mjs
```

اضبط `PORT` لتكوين منفذ الاستماع (الافتراضي: `3000`).

استخدم خط Node.js LTS الحالي لعمليات نشر الإنتاج. اعتبارًا من مايو 2026،
هو Node.js 24؛ وصل Node.js 20 إلى نهاية العمر الافتراضي في 30 أبريل 2026 ولم يعد
يتلقى تحديثات الأمان الأولية.

### عامل الميناء {#docker}

```dockerfile
FROM node:24-slim AS build
WORKDIR /app
COPY package.json pnpm-lock.yaml ./
RUN corepack enable && pnpm install --frozen-lockfile
COPY . .
RUN pnpm build

FROM node:24-slim
WORKDIR /app
COPY --from=build /app/.output .output
# data/ هو دليل SQLite تم إنشاؤه في وقت التشغيل - لا تنسخ قاعدة بيانات التطوير إلى المنتج.
# للإنتاج، قم بتعيين DATABASE_URL إلى مثيل Postgres أو Turso المستضاف.
RUN mkdir -p /app/data
ENV PORT=3000
EXPOSE 3000
CMD ["node", ".output/server/index.mjs"]
```

## فيرسل {#vercel}

```ts
// vite.config.ts
export default defineConfig({
  plugins: [agentNative({ nitro: { preset: "vercel" } })],
});
```

النشر عبر Vercel CLI أو git Push:

```bash
vercel deploy
```

بالنسبة لمساحة العمل، أنشئ كل تطبيق في حزمة Vercel Build Output API واحدة:

```bash
npx @agent-native/core@latest deploy --preset vercel
```

بالنسبة لعمليات نشر Vercel Git، قم بتعيين أمر الإنشاء على:

```bash
npx @agent-native/core@latest deploy --preset vercel --build-only
```

ينسخ بناء مساحة العمل مخرجات Nitro `vercel` لكل تطبيق إلى الجذر `.vercel/output`، ويمنح كل وظيفة بيئة مسار التثبيت الخاصة بها، ويكتب تكوين المسار الذي يخدم التطبيقات في `/<app-id>`.

## نيتليفي {#netlify}

يعمل الإعداد المسبق Nitro `netlify` بشكل جيد، وفي الممارسة العملية، أعطانا عمليات تشغيل باردة أسرع بكثير من Cloudflare Pages (حوالي 200 مللي ثانية TTFB مقابل ~9s) للقوالب التي تتحدث إلى Postgres الخارجية (نيون). إما أن تقوم بضبط الإعداد المسبق في `vite.config.ts`:

```ts
// vite.config.ts
export default defineConfig({
  plugins: [agentNative({ nitro: { preset: "netlify" } })],
});
```

...أو قم بتعيين `NITRO_PRESET=netlify` في وقت الإنشاء.

بالنسبة لمساحة العمل، انشر كل تطبيق من موقع Netlify واحد عن طريق تشغيل:

```bash
npx @agent-native/core@latest deploy --preset netlify
```

يكتب بناء مساحة العمل الأصول الثابتة ضمن `dist/_workspace_static/` ويوجه كل تطبيق إلى وظيفة Netlify الخاصة به دون عمليات إعادة توجيه الأصول القسرية، لذلك يتم تقديم ملفات مثل `/mail/assets/...` بشكل ثابت قبل أن تتعامل وظيفة الخادم مع مسارات التطبيق.

### Keep-warm وقواعد البيانات ذات التوسيع إلى الصفر {#netlify-keep-warm}

ينتج بناء Netlify دالة مُجدولة تطلب `/_agent-native/health` **كل دقيقة**. هذا يحفظ دالة الخادم وقاعدة بياناتها دافئة، حتى لا يدفع الزائر التالي بعد فترة خمول تكلفة بداية باردة (1–5 ثوانٍ على Neon). على الحوسبة المُخصّصة، هذا هو الإعداد الافتراضي الصحيح.

**على قاعدة بيانات مُحتسبة بالاستخدام مع التوسيع إلى الصفر، هذا إعداد افتراضي خاطئ.** الإيقاظ كل دقيقة يعني أن التعليق التلقائي لا يحدث أبدًا، فتُحتسب القاعدة ويُقيَّد حدّها كأنها مستيقظة باستمرار دون أن يستخدم أحد التطبيق. على الطبقة المجانية من Neon يستنفد ذلك حدّ الحوسبة، ثم ترفض القاعدة **جميع** عمليات القراءة في منتصف الجلسة — والعَرَض هو تطبيق متوقف، لا تطبيق بطيء.

| المتغير                                     | الأثر                                                                                                                                         |
| ------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------- |
| `AGENT_NATIVE_DISABLE_KEEP_WARM`            | يتخطى الدالة المُجدولة بالكامل. تصبح قاعدة البيانات حرة في التعليق التلقائي؛ ويدفع الزائر التالي بعد فترة خمول تكلفة بدايتها الباردة.         |
| `AGENT_NATIVE_KEEP_WARM_SCHEDULE`           | يستبدل الوتيرة بتعبير cron من 5 حقول، مثل `*/5 * * * *`. القيمة غير القابلة للتحليل **تُفشل البناء** بدلًا من العودة صامتةً إلى مرة كل دقيقة. |
| `AGENT_NATIVE_DISABLE_KEEP_WARM_BACKGROUND` | يُسقط تدفئة دالة الخلفية فقط ويحفظ الخادم دافئًا. تدفئة `server` طلب واحد؛ أما تدفئة `-background` فتُشغّل حاوية جديدة تعيد استقصاء المخطط.   |

إذا كنت على طبقة قاعدة بيانات مجانية أو مُقيّدة بحدّ، فاضبط `AGENT_NATIVE_DISABLE_KEEP_WARM=1` أو اختر وتيرة أبطأ.

### DDL المخطط عند الطلب {#skip-ensure-tables}

يستدعي كل store الدالة `ensureTable()` عند أول تلامس مع قاعدة البيانات، فيستقصي `information_schema` / `pg_indexes` ولا يُصدر `CREATE TABLE` / `ADD COLUMN` / `CREATE INDEX` إلا لما هو غائب فعلًا. على خادم Node طويل العمر تُدفع هذه التكلفة مرة واحدة لكل إقلاع وتكون غير مرئية. أما على serverless فإن «أول تلامس» يحدث في **كل بداية باردة**، عبر نحو 57 store — لذا قد يدفع طلب بارد مئات الرحلات المتسلسلة قبل أن يؤدي أي عمل حقيقي.

أمران يقلّلان ذلك:

- تُجاب الاستقصاءات من **مسحٍ استبطاني مجمَّع واحد لكل قاعدة بيانات** (استعلامان)، بدل استعلام لكل جدول وعمود وفهرس. يحدث هذا تلقائيًا ولا يحتاج أي إعداد.
- يتخطى `AGENT_NATIVE_SKIP_ENSURE_TABLES=1` منظومة الاستقصاء وDDL بالكامل، وهو أكبر مكسب متاح في زمن البداية الباردة.

اضبط `AGENT_NATIVE_SKIP_ENSURE_TABLES=1` عندما يكون مخطط قاعدة بياناتك مُهاجَرًا بالفعل ولا تريد أن يُنشئ إقلاع التطبيق جداول أو يعدّلها — وهي الحالة الطبيعية لنشر إنتاجي لديه خطوة هجرة حقيقية.

<Callout tone="risk">

**هذا الخيار لا يفشل مغلقًا، ولا يمكنه ذلك.** إن كان المخطط غائبًا فعلًا، سيفشل أول استعلام حقيقي بصوت عالٍ بدلًا من إصلاحه في الطريق — وهذا هو المقصود، لكنه يعني أن الهجرة مسؤوليتك. لا تضبطه على نشر يعتمد على إقلاع التطبيق لتهيئة جداوله.

</Callout>

## صفحات Cloudflare {#cloudflare-pages}

```ts
// vite.config.ts
export default defineConfig({
  plugins: [agentNative({ nitro: { preset: "cloudflare_pages" } })],
});
```

## Cloudflare Workers {#cloudflare-workers}

استخدم إخراج Nitro الأصلي للوحدات لإنشاء Cloudflare Worker مع SSR. شغّل
`npx @agent-native/core@latest build` ثم انشره باستخدام
`npx wrangler deploy --config .output/server/wrangler.json`.

```ts
export default defineConfig({
  plugins: [agentNative({ nitro: { preset: "cloudflare_module" } })],
});
```

## AWS لامدا {#aws-lambda}

```ts
// vite.config.ts
export default defineConfig({
  plugins: [agentNative({ nitro: { preset: "aws_lambda" } })],
});
```

## نشر دينو {#deno-deploy}

```ts
// vite.config.ts
export default defineConfig({
  plugins: [agentNative({ nitro: { preset: "deno_deploy" } })],
});
```

## تخزين SSR المؤقت {#ssr-caching}

كل استجابة HTML لعرض SSR وكل استجابة `.data` الخاصة بـ React Router هي غلاف واحد غير شخصي وعام: يزيل `createH3SSRHandler` ملفات تعريف الارتباط قبل العرض، لذلك تكون البايتات نفسها صحيحة لكل زائر وتحدث كل عملية التخصيص على العميل بعد التحميل. هذا ما يجعل من الآمن وضع سياسة تخزين مؤقت صارمة واحدة على هذه الاستجابات بشكل افتراضي:

```txt
cache-control: public, max-age=600, stale-while-revalidate=604800, stale-if-error=3600
```

يتم عكس نفس القيمة على `cdn-cache-control` و`netlify-cdn-cache-control`. يخدم إدخال CDN مشترك واحد الموقع بأكمله عندئذٍ بدلاً من عرض لكل طلب، وهو أكبر رافعة وحيدة لزمن استجابة أول استجابة — اتركه مفعّلاً ما لم تنطبق إحدى الحالات أدناه على عملية النشر الخاصة بك.

### التجاوز باستخدام `AGENT_NATIVE_SSR_CACHE` {#ssr-cache-env}

عيّن `AGENT_NATIVE_SSR_CACHE` في بيئة النشر لتغيير السياسة لعملية النشر بأكملها:

| القيمة                                              | النتيجة                                                                             |
| --------------------------------------------------- | ----------------------------------------------------------------------------------- |
| بدون تعيين، `on`، `default`، `true`، `1`            | السياسة الافتراضية أعلاه، دون تغيير. موصى به — أفضل أداء.                           |
| `off`، `false`، `0`، `none`، `no-store`، `disabled` | `no-store` على `cache-control`، و`cdn-cache-control`، و`netlify-cdn-cache-control`. |
| مدة زمنية: `30`، `30s`، `5m`، `2h`                  | `public, max-age=<n>, stale-while-revalidate=<n>, stale-if-error=3600`.             |

الأرقام المجردة هي بالثواني. يعكس `stale-while-revalidate` عمدًا قيمة `max-age` لمدة مخصصة: فنافذة نضارة قصيرة مقترنة بنافذة القِدَم الافتراضية البالغة سبعة أيام ستعيد بالضبط القِدَم الذي اخترت الخروج منه. تُسجِّل القيمة غير المعروفة تحذيرًا وتعود إلى الافتراضي، لذلك لا يمكن لخطأ إملائي أن يعطل CDN بصمت أبدًا.

قم بتقليله أو إيقافه عندما يكون أي مما يلي صحيحًا:

1. **مضيفك لا يقوم بتفريغ CDN الخاص به عند النشر.** تقوم Netlify وVercel بذلك؛ بعض الإعدادات ذاتية الإدارة لا تفعل. بدون تفريغ، يمكن أن يستمر إصدار تم شحنه في تقديم الغلاف السابق لمدة `max-age` بالإضافة إلى `stale-while-revalidate`.
2. **أدوات التحميل (loaders) الخاصة بك تعيد بيانات عامة قابلة للتغيير.** بعد نجاح عملية تعديل، يمكن لإعادة جلب باستخدام `useRevalidator()` أو إعادة توجيه بعد الإجراء أن تقرأ نسخة `.data` المخزنة مؤقتًا في المتصفح بدلاً من مخرجات أداة التحميل الطازجة.

ينطبق هذا الإعداد على كل سطح غلاف عام: HTML الخاص بعرض SSR في React Router و`.data`، وغلاف HTML لتسجيل الدخول، ومسار `/_agent-native/speculation-rules.json`، وموقع الوثائق، وعرض SSR للنماذج العامة في قالب النماذج. يحل Cloudflare Worker المُولَّد هذا الإعداد في وقت البناء، لذا عيّنه في بيئة النشر قبل تشغيل البناء.

ثلاثة أمور يجب مراعاتها:

- **إيقاف التخزين المؤقت لا يجعل SSR مخصصًا لكل مستخدم.** لا تزال ملفات تعريف الارتباط تُزال قبل العرض ولا تزال أدوات تحميل SSR ترى الفرع المجهول. لا يزال يجب حل بيانات كل مستخدم على جانب العميل. يتحكم هذا المتغير في _مدة_ التخزين المؤقت فقط.
- **هذا الإعداد على مستوى عملية النشر بأكملها، وليس لكل مسار، وذلك بالتصميم.** إن تجاوز التخزين المؤقت لكل مسار أو لكل طلب هو ما يجعل حمولة زائر ما تنتهي في إدخال CDN المشترك لزائر آخر، لذلك لا يوفر إطار العمل ذلك — يفرض `guard:ssr-cache-shell` هذا الأمر.
- **من أجل بيانات طازجة بعد التعديل، فضّل actions على تعطيل التخزين المؤقت.** بيانات التطبيق تنتمي إلى [actions](/docs/actions)، وتُقرأ من العميل باستخدام `useActionQuery` / `useActionMutation` وتبقى حية بواسطة استطلاع `useDbSync()`؛ لا شيء من ذلك يمر عبر ذاكرة تخزين غلاف SSR المؤقتة. اجعل أدوات تحميل SSR تعرض الغلاف العام، ولا تلجأ إلى هذا المتغير إلا عندما تريد فعليًا أن تكون بيانات أداة التحميل أكثر حداثة.

## متغيرات البيئة {#environment-variables}

### الإنشاء/وقت التشغيل {#env-runtime}

| متغير                                       | الوصف                                                                                                                                                                                                                                                                                                                                                                                                                 |
| ------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `PORT`                                      | منفذ الخادم (Node.js فقط)                                                                                                                                                                                                                                                                                                                                                                                             |
| `NITRO_PRESET`                              | تجاوز الإعداد المسبق للإنشاء في وقت الإنشاء                                                                                                                                                                                                                                                                                                                                                                           |
| `APP_BASE_PATH`                             | قم بتثبيت التطبيق تحت بادئة (مثل `/mail`). يتم ضبطه تلقائيًا بواسطة `npx @agent-native/core@latest deploy`؛ اتركه بدون ضبط للوضع المستقل.                                                                                                                                                                                                                                                                             |
| `AGENT_PROD_CODE_EXECUTION`                 | وضع تنفيذ كود الإنتاج الاختياري: `off` (افتراضي)، أو `sandboxed`، أو `trusted`. انظر [Production Code Execution](#production-code-execution).                                                                                                                                                                                                                                                                         |
| `AGENT_NATIVE_SSR_CACHE`                    | سياسة تخزين غلاف SSR المؤقت على مستوى عملية النشر بأكملها: عدم التعيين/`on` يحافظ على التخزين المؤقت الصارم الافتراضي، و`off` يرسل `no-store`، أو مدة زمنية مثل `30s` / `5m` تحدد نضارة أقصر. انظر [SSR Caching](#ssr-caching).                                                                                                                                                                                       |
| `AGENT_NATIVE_DISABLE_KEEP_WARM`            | يتخطى دالة keep-warm المُجدولة في Netlify ليتمكن قاعدة بيانات ذات التوسيع إلى الصفر من التعليق التلقائي. راجع [Keep-warm](#netlify-keep-warm).                                                                                                                                                                                                                                                                        |
| `AGENT_NATIVE_KEEP_WARM_SCHEDULE`           | يستبدل وتيرة keep-warm بتعبير cron من 5 حقول (الافتراضي `* * * * *`). القيمة غير القابلة للتحليل تُفشل البناء. راجع [Keep-warm](#netlify-keep-warm).                                                                                                                                                                                                                                                                  |
| `AGENT_NATIVE_DISABLE_KEEP_WARM_BACKGROUND` | يُسقط تدفئة دالة الخلفية فقط ويحفظ الخادم دافئًا. راجع [Keep-warm](#netlify-keep-warm).                                                                                                                                                                                                                                                                                                                               |
| `AGENT_NATIVE_DISABLE_INPROCESS_SWEEPS`     | يوقف مؤقتات المسح الاحتياطية داخل العملية (إعادة إرسال الأتمتة، تسوية agent-teams، استرداد تنفيذات البيئة المعزولة). على serverless تعمل لكل حاوية دافئة، لذا يتناسب معدل استعلاماتها مع عدد المثيلات. اضبطه **فقط** حيث يقود مُجدول دائم الاسترداد نفسه، وإلا فلن تُعاد محاولة العمل المُنتظر.                                                                                                                       |
| `AGENT_NATIVE_SKIP_ENSURE_TABLES`           | يتخطى DDL المخطط عند الطلب بالكامل. يُزيل رحلات استقصاء المخطط التي تدفعها كل بداية باردة قبل أي عمل. يتطلب أن يكون مخططك مُهاجَرًا بالفعل — راجع [DDL المخطط عند الطلب](#skip-ensure-tables).                                                                                                                                                                                                                        |
| `AGENT_NATIVE_BUILDER_RELAY_SECRET`         | سر HMAC مشترك ومخصص مكوّن من 32 حرفاً أو أكثر لترحيل تفويض Builder بأمان إلى المعاينات. عيّن القيمة نفسها في نشر رد الاتصال المؤسسي المعتمد وعمليات نشر المعاينة فقط عندما يجب أن يصل التفويض إلى قاعدة بيانات معاينة معزولة.                                                                                                                                                                                         |
| `AGENT_NATIVE_BUILDER_RELAY_TARGET_ORIGINS` | قائمة سماح مفصولة بفواصل ومخصّصة لرد الاتصال المؤسسي، وتحتوي على أصول المعاينة الموثوقة المطابقة تماماً (مثلاً `https://0123456789abcdef01234567--content.netlify.app`). عيّنها فقط في نشر رد الاتصال المؤسسي. يجب أن تستخدم أصول Netlify رابطاً دائماً غير قابل للتغيير وخاصاً بالنشر مع معرّف من 24 رقماً سداسياً عشرياً؛ وتُرفض الأسماء المستعارة القابلة للتغيير `deploy-preview-*` وأحرف البدل ولاحقات النطاقات. |

متغيرات اتصال قاعدة البيانات (`DATABASE_URL`، `DATABASE_AUTH_TOKEN`، `<APP_NAME>_DATABASE_URL` لكل تطبيق) موجودة في [Database](/docs/database#production).

### مطلوب في الإنتاج {#env-required-prod}

يجب تعيين هذه الإعدادات قبل الترويج لتطبيق ما إلى عملية نشر حقيقية. القيم المفقودة إما مغلقة بشكل فاشل (يرفض إطار العمل البدء / يرفض التعامل مع الطلبات) أو تتراجع إلى سلوك أضعف مع تحذير عالٍ.

| متغير                                       | الوصف                                                                                                                                                                                                                                         |
| ------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `BETTER_AUTH_SECRET`                        | سلسلة عشوائية من 32+ حرفًا. توقّع ملفات تعريف ارتباط الجلسة وهي بديل لـ `OAUTH_STATE_SECRET` والقيم المشفرة المحلية للتطبيق. وهي أيضًا مرشح قراءة قديم لنص خزنة مشتركة موجود. مطلوبة بشدة في الإنتاج.                                         |
| `BETTER_AUTH_URL`                           | الأصل العام لهذا التطبيق (مثل `https://mail.example.com`). يُستخدم لمجال ملفات تعريف الارتباط وبناء إعادة التوجيه OAuth.                                                                                                                      |
| `ANTHROPIC_API_KEY`                         | مفتاح API لوكيل الإنتاج المضمن. **في عمليات النشر متعددة المستأجرين**، يرفض إطار العمل الرجوع إلى ذلك عندما لا يكون لدى المستخدم مفتاح لكل مستخدم — يلزم إحضار مفتاحك الخاص. تستخدمه عمليات التثبيت المستضافة ذاتيًا لمستأجر واحد كمفتاح عام. |
| `OAUTH_STATE_SECRET`                        | مفتاح HMAC مخصص لمظاريف الحالة OAuth (Google، وAtlassian، وZoom). يعود إلى `BETTER_AUTH_SECRET` عند عدم الضبط، ولكن يوصى باستخدام قيمة مخصصة حتى لا يؤدي تدوير إحداهما إلى إبطال الأخرى. الإنشاء عبر `openssl rand -hex 32`.                  |
| `A2A_SECRET`                                | HMAC مشترك لـ A2A JSON-RPC بين التطبيقات. عند عدم ضبط مفتاح مساحة العمل المخصص، تستخدم الخزنة المشتركة مفتاحًا مشتقًا لهذا الغرض من هذه القيمة، لذا يؤدي تدويرها أيضًا إلى تدوير مفتاح الخزنة.                                                |
| `WORKSPACE_SECRETS_ENCRYPTION_KEY`          | مفتاح AES-256-GCM ثابت ومفضل لخزنة مساحة العمل المشتركة. اضبط القيمة نفسها في كل تطبيق يقرأ الخزنة؛ وهو منفصل عن تشفير بيانات اعتماد OAuth المحلية للتطبيق.                                                                                   |
| `WORKSPACE_SECRETS_ENCRYPTION_KEY_PREVIOUS` | مفتاح مساحة العمل السابق الاختياري أثناء فترة التدوير. انشره مع المفتاح الجديد، ودع القراءات تعيد تشفير الصفوف، ثم أزله بعد اكتمال الترحيل.                                                                                                   |
| `SECRETS_ENCRYPTION_KEY`                    | مفتاح قديم مشترك لتشفير التطبيق والخزنة. ما زال مدعومًا، لكن استخدم مفتاح مساحة العمل المخصص في عمليات النشر الجديدة متعددة التطبيقات.                                                                                                        |

### المصادقة والهوية {#env-auth}

تم توثيق بيانات اعتماد موفر OAuth (Google، GitHub)، والاحتياطيات لحامل MCP الثابتة (`ACCESS_TOKEN` / `ACCESS_TOKENS`)، وتبديلات التحقق من البريد الإلكتروني في [Authentication](/docs/authentication). قم بتعيينها هناك حسب وضع المصادقة الذي تختاره.

### Webhooks الوارد {#env-webhooks}

يتطلب كل تكامل للمراسلة سر التوقيع الخاص به في الإنتاج (يتم إغلاق المعالجات عند الطلبات المزورة عندما يكون السر مفقودًا). يتم سرد المتغيرات لكل تكامل في [Messaging](/docs/messaging) و[Security](/docs/security). بالنسبة للتطوير المحلي فقط، يختار `AGENT_NATIVE_ALLOW_UNVERIFIED_WEBHOOKS=1` العودة إلى "التحذير والقبول" - ولا تقم بتعيينه مطلقًا في المنتج.

### تكوين الأمان (الاشتراك) {#security-config}

الافتراضيات صارمة. تعمل مجموعة من علامات الاشتراك على تخفيف السلوك (تتبعات مكدس تصحيح الأخطاء، webhooks التي لم يتم التحقق منها، مفتاح احتياطي على مستوى مساحة العمل، محول MCP متعدد المؤسسات لمحور MCP، عمليات الكتابة env-var في وقت التشغيل). تم توثيقهم مع مقايضاتهم الأمنية في [Security](/docs/security). لا تقم بتعيينها إلا إذا كنت تريد المسار المريح على وجه التحديد.

### وراثة مساحة العمل .env {#env-inheritance}

داخل مساحة العمل، يتم تحميل الجذر `.env` في كل تطبيق تلقائيًا، لذلك يلزم تعيين المفاتيح المشتركة مثل `ANTHROPIC_API_KEY` و`A2A_SECRET` و`BETTER_AUTH_SECRET` و`OAUTH_STATE_SECRET` مرة واحدة فقط. يفوز `apps/<name>/.env` لكل تطبيق في الصراع.

### توليد أسرار قوية {#env-generate-secrets}

بالنسبة لأي سر تم وضع علامة "32+ حرف عشوائي" (`BETTER_AUTH_SECRET`، `OAUTH_STATE_SECRET`، `A2A_SECRET`، `WORKSPACE_SECRETS_ENCRYPTION_KEY`، `SECRETS_ENCRYPTION_KEY`، `AGENT_NATIVE_BUILDER_RELAY_SECRET`)، قم بإنشاء قيم جديدة باستخدام:

```bash
openssl rand -hex 32
```

قم بتدويرها عن طريق استبدال env var في كل مثيل وإعادة النشر - تصبح مظاريف حالة الجلسات / OAuth الموقعة تحت المفتاح القديم غير صالحة، لذلك قد يحتاج المستخدمون إلى تسجيل الدخول مرة أخرى.

## أدوات وكيل الإنتاج {#production-agent-tools}

يحصل وكلاء الإنتاج على أدوات إطار العمل actions بالإضافة إلى أدوات إطار العمل المسجلة للتطبيق من
المكون الإضافي للدردشة مع الوكيل. يتم تمكين فحص SQL افتراضيًا، بينما تمر
عمليات الكتابة عبر actions التطبيق المكتوبة ما لم يشترك التطبيق صراحة في أدوات الكتابة الخام:

```ts
// server/plugins/agent-chat.ts
export default createAgentChatPlugin({
  frameworkTools: {
    database: "read", // "write" | "read" | "off" — default "read"
    extensions: true, // default false
  },
});
```

```ts
// server/plugins/core-routes.ts
export default createCoreRoutesPlugin({
  extensionTools: true,
});
```

- `database: "read"` — افتراضي. يُسجل `db-schema` و`db-query` فقط؛
  يفحص الوكلاء البيانات باستخدام SQL ويجب أن يستخدموا actions التطبيق المكتوبة للكتابة.
- `database: "write"` أو `true` — يسجل أيضًا `db-exec` و`db-patch`
  للصيانة الخام المقصودة. يتم تحديد نطاق عمليات الكتابة للمستخدم/المؤسسة الحالية
  ويتم حظر تغييرات المخطط.
- `database: "off"` أو `false` — يزيل أدوات قاعدة البيانات الأولية من
  سطح الوكيل، لذا فإن actions الخاص بالتطبيق هو المسار الوحيد للوصول إلى البيانات.
- `extensions: true` — يفعّل للتطبيق actions وإرشادات إدارة امتدادات
  إطار العمل (`create-extension` و`update-extension` وغيرها). اضبطه على كلا
  المكوّنين الإضافيين للسماح أيضًا بإنشاء REST الموثق. تكون الامتدادات معطلة
  افتراضيًا.

بقية المجموعات قيم منطقية ومفعّلة افتراضيًا: `sharing`, `review`, `history`, `featureFlags`, `localization`, `audit`, `contextXray`, `userProfile`, `automation`, `docs`, `resources`, `web`, `workspaceApps`, `chat`, `email`.
استخدم `frameworkTools: "minimal"` لإيقافها كلها مرة واحدة في تطبيق صوتي أو
أحادي الغرض، أو `frameworkTools: { preset: "minimal", resources: true }`
للإبقاء على واحدة فقط — فمفتاح المجموعة الصريح يتقدّم دائمًا على الإعداد المسبق.

إيقاف مجموعة يزيلها من أسطح **الوكيل** فقط: المحادثة التفاعلية وMCP وA2A
والتشغيل في الخلفية. أما مسارات HTTP الخاصة بتلك actions فتبقى مثبّتة، لأن واجهة
المستخدم تستدعيها عبر hooks العميل — فإيقاف `sharing` لا يجوز أن يُنتج 404 في
نافذة مشاركة لا تزال ظاهرة. وازن هذه التكلفة أولًا: عقد الإطار هو أن كل ما تقدر
عليه الواجهة يقدر عليه الوكيل، لذا فإن `sharing: false` مع زر مشاركة ظاهر هو
خروج متعمّد عن ذلك، ولا يصح إلا إذا كان التطبيق يخلو من ذلك السطح أصلًا.

وبمعزل عن هذا الخيار، لم تعد أدوات الإطار تُحمَّل افتراضيًا في أول طلب للنموذج.
فهي تبقى في السجل القابل للبحث وتُحمَّل عبر `tool-search`، أو فورًا إذا سمّاها
التطبيق في `initialToolNames`.

<Callout tone="warning">
  الخياران `databaseTools` و`extensionTools` في المستوى الأعلى مهملان لصالح
  `frameworkTools.database` و`frameworkTools.extensions`. وما زالا يعملان، لكن
  ضبط خيار في المستوى الأعلى ونظيره في `frameworkTools` على قيمتين متعارضتين
  يرفع خطأً عند بدء تشغيل plugin بدلًا من الإقلاع بسطح أدوات لم يختره أحد.
</Callout>

## تنفيذ كود الإنتاج {#production-code-execution}

افتراضيًا، يعمل وكلاء الإنتاج بدون أدوات تنفيذ التعليمات البرمجية. يمكنهم استدعاء التطبيق actions، وأدوات قاعدة البيانات، وأدوات MCP، وأدوات المتصفح/الجلسة، وأدوات إطار العمل المسجلة الأخرى، لكنهم لا يحصلون على حق الوصول إلى نظام الملفات أو shell.

يمكن لعمليات النشر المتوافقة مع العقدة الاشتراك في تنفيذ تعليمات برمجية للإنتاج من خلال البرنامج الإضافي لدردشة الوكيل أو تجاوز البيئة:

```ts
// server/plugins/agent-chat.ts
export default createAgentChatPlugin({
  codeExecution: { production: "sandboxed" },
});
```

الأوضاع المتاحة هي:

- `off` — الإعداد الافتراضي. لم يتم تسجيل أي أدوات لتنفيذ التعليمات البرمجية في الإنتاج.
- `sandboxed` — تسجيل `run-code`، وهو مشغل Node.js JavaScript معزول مع بيئة منقحة، ودليل مؤقت جديد، وحدود الإخراج/الوقت، وجسر مضيف محلي للأدوات المسجلة المدرجة في القائمة المسموح بها مثل `provider-api-request`، و`provider-api-docs`، و`provider-api-catalog`، و`web-request`، وجسر ملفات مساحة العمل المدعومة بالموارد. المستخدمة من قبل `workspaceRead` / `workspaceWrite`.
- `trusted` — يقوم بتسجيل `run-code` بالإضافة إلى سجل أدوات الترميز الكامل (`bash`، `read`، `edit`، `write`). استخدم هذا فقط لعمليات النشر التي يتحكم فيها مستأجر واحد أو المشغل حيث يكون الوصول الكامل إلى المضيف مقصودًا.

قم بتعيين `AGENT_PROD_CODE_EXECUTION=sandboxed` أو `AGENT_PROD_CODE_EXECUTION=trusted` لتجاوز خيار البرنامج الإضافي لنشر محدد دون تغيير التعليمات البرمجية. يفرض `AGENT_PROD_CODE_EXECUTION=off` إيقاف تنفيذ التعليمات البرمجية حتى عندما يمكّنه خيار البرنامج الإضافي.

إن صندوق الحماية `run-code` عبارة عن عزل على مستوى العملية، وليس حاوية نظام تشغيل. فهو يزيل أسرار التطبيق من بيئة العمليات التابعة ويستخدم نموذج إذن Node عندما يكون متاحًا، ولكن الشبكة الصادرة لا يتم حظرها بواسطة Node نفسها؛ يجب أن تمر المكالمات التي تمت مصادقتها عبر مساعدي الجسر الذي تكشفه الأداة.

## تحديث UI في الإنتاج {#updating-ui-in-production}

يعمل وكيل التطبيق المنشور العادي عبر أدوات التطبيق - actions وقاعدة البيانات وMCP والتكاملات المكوّنة - بدلًا من الحصول على وصول ضمني إلى مصدر التطبيق. تنتمي تغييرات المصدر إلى سير عمل تطوير المستودع. يمكن لإطار مضمّن أو محلي أو Builder تحرير المستودع عندما يُمنح عمدًا أدوات مساحة العمل والكتابة.

في نشر الإنتاج القياسي مع إيقاف [production code execution](#production-code-execution)، يتمتع الوكيل بإمكانية الوصول إلى أدوات التطبيق (actions، وقاعدة البيانات، وMCP) ولكن ليس إلى نظام الملفات. وهذا يعني أن الوكيل يمكنه قراءة البيانات وكتابتها، وتشغيل actions، والتفاعل مع الخدمات الخارجية - ولكن لا يمكنه تحرير مكونات React أو إضافة مسارات جديدة على مثيل منشور.

### Builder.io: التحرير المرئي في الإنتاج {#builderio}

[Builder.io](https://www.builder.io?utm_source=agent-native&utm_medium=docs&utm_campaign=framework&utm_content=deployment) هو سير عمل منفصل مُدار وقادر على تعديل التعليمات البرمجية، ويمكن للوكيل فيه تعديل UI لتطبيقك في الإنتاج. صِل مستودعك بـ Builder.io (free tier available) واطلب تغييرات UI مباشرة - لا حاجة لإعادة النشر.

**كيفية العمل:**

1. قم بتوصيل الريبو الأصلي للوكيل الخاص بك إلى Builder.io (free tier available)
2. يوفر Builder.io إطارًا سحابيًا مع الوكيل والتحرير المرئي والتعاون في الوقت الفعلي
3. اطلب من الوكيل إجراء تغييرات على UI - حيث يقوم بتحرير المكونات والمسارات والأنماط بشكل مباشر
4. يتم الالتزام بالتغييرات مرة أخرى في الريبو الخاص بك

راجع [Frames](/docs/frames) لمعرفة المزيد حول لوحة الوكيل المضمنة مقابل خيارات الإطار السحابي.

## نشر المثيلات المتعددة {#multi-instance}

تقوم تطبيقات Agent-Native بتخزين جميع الحالات في SQL عبر Drizzle ومزامنة UI عبر [polling](/docs/key-concepts#polling-sync) مع قاعدة البيانات - لا توجد حالة نظام ملفات، ولا توجد جلسات ثابتة، ولا توجد ذاكرة تخزين مؤقت في الذاكرة. وهذا يعني أن عمليات النشر متعددة المثيلات وبدون خادم تعمل خارج الصندوق: قم بتوجيه كل مثيل إلى نفس `DATABASE_URL` وستتقارب تلقائيًا. انظر [Key Concepts — Data in SQL](/docs/key-concepts#data-in-sql) و[Portability](/docs/key-concepts#hosting-agnostic).

## الخطوات التالية

- [**Database**](/docs/database#production) — اختر قاعدة بيانات SQL دائمة قبل أول نشر للإنتاج
- [**Security**](/docs/security) — قائمة تحقق الإنتاج والعلامات الاختيارية المشار إليها في هذه الصفحة
- [**Authentication**](/docs/authentication) — بيانات اعتماد OAuth ومتغيرات البيئة لوضع المصادقة الذي تختاره
- [**Multi-App Workspace**](/docs/multi-app-workspace) — نموذج مساحة العمل وراء بنية `deploy` ذات الأصل الواحد
- [**Frames**](/docs/frames) — لوحة الوكيل المضمنة مقارنةً بإطار Builder.io السحابي لتعديلات واجهة الإنتاج
