# Custom health check message
PING_MESSAGE=pong

# Skip login/signup (local dev / preview only)
# AUTH_DISABLED=true

# The committed agent-native.json makes local development show the shared
# Connect Builder / Add your own keys choice and skip the generic catalog.
