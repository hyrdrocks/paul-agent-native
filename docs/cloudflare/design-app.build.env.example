# Build-time environment for hyrdrocks/paul-design-app.
#
# The Cloudflare Worker preset GENERATES .output/server/wrangler.json. These
# variables are the only way the five bindings get into it. The queue's NAME is
# still derived from the Worker's name, but as of #30 the emit is conditional
# and must be declared here.
#
# Values are real and provisioned (see design-app-resources.md). A database id
# is an identifier, not a credential. No secret belongs in this file — the
# provider key and the auth secrets are set with `wrangler secret put` for the
# deploy and in `.cf-local/dev.vars` for the local Workers run.

NITRO_PRESET=cloudflare_module

# Both, or neither. resolveCloudflareD1Binding() throws on exactly one, and
# returns null on neither — which silently means "use DATABASE_URL" and leaves
# the Worker on the SQLite path.
CLOUDFLARE_D1_DATABASE_NAME=paul-design-app
CLOUDFLARE_D1_DATABASE_ID=256288ec-77ac-4e9d-ab1b-8d415e4ee997

# Omitting this is not an error at build or deploy time: the UPLOADS binding is
# simply not emitted and the first upload fails closed at runtime. The local
# `just cf` build needs it too, or local and deployed take different paths.
CLOUDFLARE_R2_BUCKET_NAME=paul-design-app-uploads

# Browser Rendering is an account entitlement with no resource to name, so this
# declares intent rather than pointing at something. Omitting it is not an error
# at build or deploy time: no `browser` key is emitted and the first render
# fails closed at runtime, naming this variable and the BROWSER binding. Set it
# on the local `just cf` build too, or local renders fail while deployed ones
# work.
CLOUDFLARE_BROWSER_RENDERING=1

# Declares that paul-design-app-agent-background and its -dlq exist. Omitting
# this is NOT the same as the other three: a build that still wants durable
# background runs (AGENT_CHAT_DURABLE_BACKGROUND unset or truthy, the default)
# is refused at build time naming both queues, because a Worker with no queue
# runs every background turn inline under the foreground clamp while looking
# healthy. Set AGENT_CHAT_DURABLE_BACKGROUND=false instead to mean that
# deliberately.
CLOUDFLARE_BACKGROUND_QUEUE=1
